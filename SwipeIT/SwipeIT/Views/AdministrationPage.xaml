<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SwipeIT.ViewModels"
             x:Class="SwipeIT.Views.AdministrationPage">
    <ContentPage.BindingContext>
        <local:AdministrationViewModel />
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout Orientation="Vertical" HorizontalOptions="CenterAndExpand">
            <StackLayout Orientation="Horizontal">

                <Label Text="&#xf505;"
                       FontSize="Large"
                       FontFamily='FontAwesome'
                       TextColor="Blue" />
                <Label Text="Administration" />
                <Button Text="&#xf2f5;"
                        FontFamily='FontAwesome'
                        HorizontalOptions="EndAndExpand"
                        Command="{Binding LogoutCommand}" />
            </StackLayout>

            <CollectionView ItemsSource="{Binding DateLogs}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Label Text="{Binding DateModified}" FontSize="Medium" />
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>