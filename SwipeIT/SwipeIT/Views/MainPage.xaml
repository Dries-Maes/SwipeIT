<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:SwipeIT.ViewModels"
             x:Class="SwipeIT.Views.MainPage">
    <ContentPage.BindingContext>
        <local:MainViewModel />
    </ContentPage.BindingContext>
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="{Binding CurrentUser.Name }" />
        <ToolbarItem Text="Like"
                     Command="{Binding Like}" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>

            <CarouselView  ItemsSource="{Binding DevelopersResult}">
                <CarouselView.ItemTemplate>
                    <DataTemplate>
                        <SwipeView>
                            <SwipeView.TopItems>
                                <SwipeItems>
                                    <SwipeItemView>
                                        <StackLayout Margin="10"
                                                     Orientation="Horizontal"
                                                     HorizontalOptions="Center">
                                            <Button Text="Like"
                                                    BackgroundColor="Green" />
                                            <Button Text="Button2"
                                                    BackgroundColor="Red" />
                                        </StackLayout>
                                    </SwipeItemView>
                                </SwipeItems>
                            </SwipeView.TopItems>

                            <StackLayout Padding="10">
                                <Label Text="{Binding  Name }"
                                       FontSize="16" />
                                <Label Text="{Binding ID}"
                                       LineBreakMode="NoWrap"
                                       FontSize="13" />
                                <Image Source="{Binding Image}"
                                       WidthRequest="200"
                                       HeightRequest="200" />
                            </StackLayout>
                        </SwipeView>
                    </DataTemplate>
                </CarouselView.ItemTemplate>
            </CarouselView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>